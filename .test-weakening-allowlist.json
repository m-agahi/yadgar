{
  "_comment": "Allowlist for scripts/check_test_weakening.py (Layer 4, #52). Replaced the ALLOW_TEST_WEAKEN=1 environment override on 2026-08-08 — that was a blanket, invisible, whole-run bypass that silenced every file at once, left no trace in the reviewed diff, and was wired to a PR label in both CI workflows so it could be set from outside the repo. Each entry sanctions ONE file's deletion with an EXACT delta and a written reason. Governance mirrors .health-endpoint-allowlist.json / .urllib-httperror-close-allowlist.json: rationale >= 40 chars, malformed entries hard-fail. Two rules are specific to this guard: (1) an entry grants exactly its recorded allowed_delta — a measured delta WORSE than the entry fails, so an entry can never absorb future weakening of the same file; (2) a file with no entry fails as before. DO NOT widen an allowed_delta to make a new failure go away: that converts a one-time sanction into a permanent licence for the file, which is the hole this mechanism replaced.",
  "_stale_policy": "A STALE entry (file absent from the branch diff, or measured delta better than recorded) is a WARNING printed on every run, NOT a hard error — deliberately unlike the sibling allowlists, whose input is the filesystem. This guard diffs against merge-base(origin/master, HEAD), which MOVES: the moment a branch carrying an entry merges, its file leaves the diff and the entry goes stale through nobody's fault, and hard-failing would turn master red for everyone. The two ADR-0215 train entries this file used to carry (test_scope_filter_e2e.py, test_v5_42_2_branch_default_e2e.py) were removed on 2026-08-12 by 0047 C15a under exactly that instruction: the train had merged, both files were absent from BOTH merge-base 92dbb4b5 and HEAD, so neither could ever re-enter the branch diff and neither entry could ever stop warning. An entry whose staleness is unresolvable is noise that teaches the next reader to skim the warnings — delete it as this policy directs, and never widen an allowed_delta to silence one.",
  "_entries": "Ten entries. Task 379 widened the scan from the e2e subset to every yadgar/tests/**/*.py, which made the guard capable of seeing the relaxation it was filed for; recording those three was the point of the widening. Car K (ADR-0464, 2026-08-28) added seven more, all of one shape: it deleted four provably-dead code chains, and a test that PINNED a deleted subject dies with it. Each entry states which subject went and what coverage survived — two of the seven are whole-file deletions, and every test in the set that asserted real behaviour was repointed onto the surviving code rather than dropped.",
  "yadgar/tests/scripts/test_migration_014_wiki_embedding_backfill.py": {
    "allowed_new_skips": 5,
    "rationale": "Gate-honesty car I: FALSE RED removed, not coverage. These five tests assert a NON-ZERO backfill count, which requires a real embedding vector; sentence-transformers lives in the optional [ml] extra, so under plain `make test` (`--extra test`) EmbeddingEngine returned None for every row and they measured `assert 0 == 4` / `0 == 1` / `0 == 6`. Measured 2026-08-27: 5 failed / 13 passed on a venv synced `--extra dev --extra test`; 19 passed (this file + the sibling recall test) on one synced `--extra dev --extra test --extra ml --extra sql --extra analytics`. Standing failures that are not bugs train readers to skim red — the mirror of the false green this train exists to kill. The guards are pytest.importorskip('sentence_transformers') and are sanctioned CONDITIONALLY in yadgar/tests/skip_inventory.json (ml-extra-embedding-backfill-01, `sanctioned_when_module_absent`), so the skip gate FAILS a leg that installed the extra and skipped them anyway — this cannot hide a regression. Verified both directions: the same skip lines under a MEASURED `sentence_transformers=present` receipt exit 1. Deliberately scoped to the five that actually failed, not the whole class: test_returns_zero_on_empty_db and test_backfill_embed_failure_warns_and_skips pass on their own merits without the extra and still run under `make test`. ADR-0087 governs the inventory side."
  },
  "yadgar/tests/scripts/test_v5_42_5_directory_contract.py": {
    "allowed_new_skips": 1,
    "rationale": "Gate-honesty car I, same mechanism, one test. test_every_candidate_query_carries_the_scope_predicate ends with four liveness assertions that stop `no unscoped queries` being vacuously true; one requires the memory VECTOR arm to have issued its query, which needs a real query embedding from the [ml] extra. Measured absent it failed with `memory vector arm did not run` on every plain `make test` run — false RED with no bug behind it. TRADE-OFF recorded rather than hidden: the guard skips the WHOLE test, so the SQL-level scope-predicate assertion also stops running under plain `make test`. Making only the vector-arm assertion conditional would be a weakened assertion, which is worse, and CI plus `make test-ci` install `--extra ml` so the check still runs where it gates merges. Sanctioned CONDITIONALLY in yadgar/tests/skip_inventory.json (ml-extra-recall-vector-arm-01): the skip gate FAILS if this ever skips on a leg where sentence_transformers is measured PRESENT."
  },
  "yadgar/tests/backend/test_project_registry.py": {
    "allowed_delta": -13,
    "allowed_strict_delta": -11,
    "rationale": "FILE DELETED, not weakened. car/T-dead-code-purge removed yadgar/backend/admin_exec/project_registry.py entirely: _ensure_project_exists_async and _ensure_project_exists_sync had ZERO production call sites (Car 5 proved it, and identity_stamp.py documented it), and what remained was a _live_engine used only by that pair plus a re-export of two error classes _shared/storage/sql/errors.py already owns. The live registry guard is MariaStorageEngine.assert_project_registered (_shared/storage/sql/registry.py), which is covered by test_c6_project_registry_writer.py and the new refresh=False preview tests. Deleting a test whose subject no longer exists is not a coverage loss; this entry records that judgement rather than hiding it. Ledger task 384."
  },
  "yadgar/tests/_shared/test_models_module.py": {
    "allowed_delta": -66,
    "allowed_strict_delta": -65,
    "rationale": "PINS-THE-DELETED-SUBJECT, not a relaxation. Car K (ADR-0464) deleted five pydantic classes from yadgar/_shared/contracts/models.py — Episode, Memory, ProspectiveMemory, NarrativeEntry, Checkpoint — after verifying they have ZERO production importers (the module's only non-test production import is core/server/tools/adr_render.py:14, which imports ADR alone). This file's TestEpisode / TestMemory / TestProspectiveMemory / TestNarrativeEntry / TestCheckpoint classes instantiated those classes and asserted their field defaults; every assertion removed here names a class that no longer exists. The classes covering the SURVIVING models (Entity, Relationship, ConsolidationLog, FileHash, MemoryStats, MemoryCluster, AstrocyteProcess, MemoryRule, MemoryArchive, MemoryTransition, CausalDAGEdge, ADR) are untouched."
  },
  "yadgar/tests/_shared/test_frontier_schema.py": {
    "allowed_delta": -26,
    "allowed_strict_delta": -26,
    "rationale": "PINS-THE-DELETED-SUBJECT. Same Car K (ADR-0464) deletion: TestMemoryModelFields asserted default values on the pydantic `Memory` class, which is gone. The file's CRUD tests build plain dicts via _make_memory() and reach the real storage engine — they never touched the pydantic class and are untouched here."
  },
  "yadgar/tests/_shared/test_bitemporal_extension.py": {
    "allowed_delta": -1,
    "allowed_strict_delta": -1,
    "rationale": "PINS-THE-DELETED-SUBJECT, and one test REPOINTED rather than dropped. Car K (ADR-0464) deleted storage/user.py::get_profiles_for_entity and storage/narrative.py::get_beliefs_for_subject (zero production callers; user_profile and derived_belief both hold 0 rows). The removed assertions were the 'still works' smoke calls on those two dead readers inside test_existing_insert_profile_callers_pass_unchanged / test_insert_belief_via_record_works; the live readers those tests also exercise (search_profiles_fts, search_beliefs_fts) keep their assertions. Separately, test_get_beliefs_for_subject_default_excludes_invalidated was NOT deleted — it was repointed onto search_beliefs_fts (the surviving reader with the same include_invalidated semantics) and GAINED a positive control asserting the FTS query matches the seeded row with include_invalidated=True, without which the default-exclusion assertion would pass for free whenever the query simply matched nothing. Belief invalidation coverage is therefore stronger than before, not merely preserved."
  },
  "yadgar/tests/core/test_staleness.py": {
    "allowed_delta": -7,
    "allowed_strict_delta": -6,
    "rationale": "PINS-THE-DELETED-SUBJECT. Car K (ADR-0464) deleted the StalenessDetector watchdog arm — start/stop/_FileChangeHandler/scan_directory/_is_binary — after verifying the only production caller of the chain, core/server/_startup.py, passed watch_directory=None, so the observer was never scheduled and scan_directory had zero non-test callers. TestScanDirectory and TestStaleMemoryHeat::test_scan_halves_heat exercised scan_directory only. validate_memory, _compute_file_hash, file-deletion and heat-halving coverage all survive."
  },
  "yadgar/tests/core/test_staleness_handler_merged.py": {
    "allowed_delta": -2,
    "allowed_strict_delta": -2,
    "rationale": "FILE DELETED, not weakened. Car K (ADR-0464) removed _FileChangeHandler with the rest of the unreachable watchdog arm; this module tested nothing else (every test constructs _FileChangeHandler and asserts on_created/on_modified delegation)."
  },
  "yadgar/tests/scripts/test_car_d_rekey_migration.py": {
    "allowed_delta": -76,
    "allowed_strict_delta": -61,
    "rationale": "FILE DELETED, not weakened. Car K (ADR-0464) removed yadgar/core/migrations/ (the whole package — rekey_corpus was its only module) and the `yadgar migrate rekey` CLI arm: the corpus re-key is a spent one-shot migration and both tables it re-keys (memory, wiki_page) are now 0 rows unstamped. This module tested rekey_corpus exclusively. The one test in the tree that used `migrate rekey` as a VEHICLE for live behaviour — test_main_module.py's exit-code-propagation test — was repointed onto `snapshot restore` rather than deleted."
  },
  "yadgar/tests/server/test_integration.py": {
    "allowed_delta": -6,
    "allowed_strict_delta": -4,
    "rationale": "PINS-THE-DELETED-SUBJECT. Car K (ADR-0464): TestStalenessWatcherIntegration asserted detector.start()/stop()/is_running and scan_directory — the watchdog arm this car deleted as unreachable. Nothing else in the file changed."
  }
}

[
  {
    "path": "yadgar/core/server/tools/adr_render.py",
    "function": "_assemble_index_rows",
    "lineno": 170,
    "metrics": {
      "cyclomatic": 18
    },
    "rationale": "Car G (0047 \u00a77 \u00a74.6): _assemble_index_rows cyclomatic moved from the legacy ~13 (parse_index_rows branch + per-target superseded_by append) to 18 after the re-point off the wiki-index page onto the SQL ledger via _forward_admin('list_adr_rows'). The added branches are irreducible: ledger-fetch try/except (degraded mode when forward fails), per-row dict/type guard (the ledger may carry stale rows), supersede-back-link computation (Car F re-pointed this onto in-memory ledger rows), and the per-target status flip with multi-superseder accumulation (a target superseded multiple times before the seed landed carries a comma-joined superseded_by list). Splitting into _fetch_ledger_rows + _apply_supersede_back_links + _project_consumer_shape would reduce cyclomatic but at the cost of three round-trips through the call site. Same shape as Car F's WikiStore.add (cyclomatic=16, 2026-08-09) and Car G's seed_adr_rows (cyclomatic=23, 2026-08-09).",
    "added": "2026-08-09",
    "added_by": "car-g-adr-seed"
  },
  {
    "path": "yadgar/backend/admin_exec/adr_seed.py",
    "function": "seed_adr_rows",
    "lineno": 714,
    "metrics": {
      "fn_loc": 204
    },
    "rationale": "Car G (0047 \u00a77): seed_adr_rows cyclomatic moved from the legacy ~10 (ad-hoc page-by-page loop with parse_index_rows fallback) to 23 after the D35a/D35b re-point to per-ADR pages and the async storage refactor that put list/create/link/supersede on the ledger directly (the layer-boundary cleanup removed the _forward_admin bridge \u2014 every storage call now carries its own await + try/except guard). The branches are irreducible: idempotency check (body_slug existing), per-page title/status/date parse, body_writer/row_inserter/slug_linker injector selection, supersede-target extraction + add_adr_supersedes forward, page-id\u2194row-id resolution, gate predicate on success, type guards on storage returns (async vs sync stub), result-shape narrowing \u2014 all part of the seed's stated contract. Splitting into _seed_one_page + _finalize_seed helpers would still need the same branches somewhere; cyclomatic wouldn't reduce. | Re-baselined 2026-08-21 (car 19, ledger task 176): fn_loc 193 -> 204 (+11). The growth is 9 docstring lines and 2 code lines \u2014 the dry-run preflight call plus the `if structural_error is None:` guard that skips the insert loop when the write path's guards already rejected the input. The BRANCH count is why this is not decomposable away: the whole point is that the dry-run path and the apply path must pass through the SAME validation, so hoisting it into a helper (which is what _preflight_write_guards already is) cannot remove the call site or the guarded loop. Reviewed on its merits, unlike a bare ratchet re-baseline.",
    "added": "2026-08-09",
    "added_by": "car-g-adr-seed"
  },
  {
    "path": "yadgar/core/server/tools/wiki.py",
    "function": "wiki_add",
    "lineno": 362,
    "metrics": {
      "fn_loc": 228
    },
    "rationale": "Car B0 (#83): wiki_add was already at fn_loc=148 (2 lines under the 150 HARD cap) before this change. Persisting repo-wiki module staleness metadata requires 2 new signature params (hash, source_file) + 2 payload dict entries + a 2-line docstring note \u2014 all irreducible feature surface, not logic branching (cyclomatic unchanged). Net +6 crosses the cap. The honest fix is extracting the payload-dict/branch-resolution preamble into a helper; that decomposition is a wiki-tools refactor of its own, not in scope for the B0 prerequisite. Car M (0047 spine train, 2026-08-09): fn_loc 154 -> 190 for the cross-project project= override (resolve_effective_project wiring + debug docstring block + payload stamping). Cyclomatic and params unchanged (the project= param is keyword-only, defaults to None, takes 0 lines in the argument list). Same deferral: the parameter is essential feature surface, not logic branching; extracting payload-dict resolution into a helper is the wiki-tools refactor's job, not in scope for Car M. Re-baselined 2026-08-20 from fn_loc=190->194 (+4) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-20 on the signals-that-lie train merge: fn_loc 194 -> 211 (+17). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size. | Re-baselined 2026-08-21, car/wiki-truncation-guard (ledger task 271): fn_loc -> 223 (+12) for ``allow_truncation``. Same shape as every entry above it: essential parameter surface plus its contract prose, not logic branching -- cyclomatic is unchanged. Eight of the twelve lines are the docstring paragraph that stops a caller reaching for ``force`` instead (a different gate, with a different consequence). Recorded so the ratchet catches FUTURE drift; the payload-dict extraction this entry has named since Car M remains the real remedy. | Re-baselined 2026-08-22, train/C0 (wiki_add(project=...) satisfies the directory gate): fn_loc 223 -> 228 (+5). The +5 is the gate-return unpacking and the if/else that reuses the resolved id to skip the redundant resolver call when project= alone satisfied the gate. Same shape as every entry above it: essential feature surface (Car M project= override must reach the resolver even when the MCP caller cannot supply directory=), not logic branching -- cyclomatic unchanged. The resolve-before-reject logic itself lives in the gate helper, which the call site delegates to. Recorded so the ratchet catches FUTURE drift; the payload-dict extraction this entry has named since Car M remains the real remedy.",
    "added": "2026-07-21",
    "added_by": "repo-wiki-refresh-car-b0"
  },
  {
    "path": "yadgar/_shared/observability/log_config.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1029
    },
    "rationale": "Folder-split #17 Car 4b: rewriting call-site imports to the real layer paths made several import statements longer (yadgar.tracing -> yadgar._shared.tracing, etc.); ruff-format wrapped one multi-symbol import across parenthesized lines, pushing file_loc from ~999 to 1002, just over the 1000 HARD cap. Net +3 lines are import-path formatting, not logic; module's essential complexity is unchanged. Splitting to shed 3 lines is churn with no readability gain. Re-baselined 2026-08-20 from file_loc=1002->1029 (+27) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-07-07",
    "added_by": "reorg-car4b"
  },
  {
    "path": "yadgar/core/vacuum/__init__.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 2061
    },
    "rationale": "P0 #37 safe-stop train (2026-07-10): +~265 LOC for the swap quiescence gate, finalize rollback-on-unverified, and the /proc inode-coherence scan (all with incident-context docstrings \u2014 the 07-09 split-brain postmortem lives next to the code it guards). Prior entry: I33 obs rollout 1015. The honest fix is splitting finalize/guards out of __init__ \u2014 that restructure belongs to the parked T2 layer-boundary train (#5), not a P0 incident-fix branch. Car 0092 (2026-07-31): 1280 -> 1713. +~430 LOC for the side-build surreal-binary preflight, the named-skip-reason consolidation_log path, and the abort-path .pre-vacuum prune -- again incident-context docstrings carrying the wedge postmortem (export + stop-both-units + full copytree ran before a FileNotFoundError, and no abort path pruned the snapshot it had just made). The honest fix remains splitting finalize/guards/preflight out of __init__ into siblings of phases.py; that restructure still belongs to the parked T2 layer-boundary train (#5), not a bug-train car. Car 0092 full fix (2026-08-01): 1713 -> 1739. The side-build LAUNCHER SEAM itself was deliberately put in a NEW sibling (yadgar/core/vacuum/launcher.py) rather than added here, and _stop_side_backend_clean MOVED there with it -- so the container branch cost this file +26 net LOC (the broadened preflight and its incident-context docstring), not +250. Car 0113 (2026-08-01): 1739 -> 1974. +~235 LOC for the write-quiescence seam (the maintenance enter/exit/release helpers, the cross-process queue-drain nudge, and the incident-context block explaining WHY the exact-count gate is structurally blind to a write landing between the export and the backend stop). The seam is deliberately here rather than in a sibling: it wraps cmd_vacuum_impl's existing sensitive-lock try/finally, which lives in this file, and splitting only the wrapper out would put the lifecycle on one side of a module boundary and the lock it must not skip on the other. The honest fix remains splitting finalize/guards/preflight out into siblings of phases.py, still parked on the T2 layer-boundary train (#5). Car 0046 (2026-08-01): 1974 -> 2046. +72 net LOC, almost entirely the incident-context docstrings on the single reap site (_reap_vacuum_residue carries the reasoning of the SEVEN scattered call sites it replaces, including Car 0092's low-disk wedge argument, so deleting those calls cannot be misread as regressing it). The retention MECHANICS deliberately went to phases.py next to _run_cleanup_script (_reap_export_pairs, _reap_snapshots_by_age, the stamp parser), not here; what stays is the wrapper, which \u2014 like 0113's \u2014 must live beside the try/finally and the lock it may not skip. Two removals partly offset it: seven reap call sites and their comment blocks, and _vacuum_finalize's keep_n param (which retired that function's params=9 allowlist entry outright). Re-baselined 2026-08-20 from file_loc=2046->2055 (+9) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-20 on the signals-that-lie train merge: file_loc 2055 -> 2061 (+6). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size.",
    "added": "2026-07-10",
    "added_by": "safestop-car2"
  },
  {
    "path": "yadgar/backend/causal_discovery/pc.py",
    "function": "pc_algorithm",
    "lineno": 105,
    "metrics": {
      "cyclomatic": 53,
      "fn_loc": 175,
      "nesting": 6
    },
    "rationale": "PERMANENT. Textbook PC constraint-based causal-discovery algorithm; the branching structure (skeleton phase, v-structure orientation, Meek propagation) is the algorithm itself. Extraction scatters the invariants across helpers and harms readability without reducing essential complexity.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "hook_auto_capture",
    "lineno": 393,
    "metrics": {
      "cyclomatic": 19
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor Re-baselined 2026-08-20 from cyclomatic=16->19 (+3) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "hook_session_context",
    "lineno": 488,
    "metrics": {
      "cyclomatic": 18,
      "nesting": 5
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "hook_prompt_recall",
    "lineno": 603,
    "metrics": {
      "cyclomatic": 21
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor. Car 7 (2026-08-13) resynced cyclomatic 17 -> 20 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. Re-baselined 2026-08-20 from cyclomatic=20->21 (+1) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "_handle_team_inbox",
    "lineno": 954,
    "metrics": {
      "cyclomatic": 16
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "_make_event_stream",
    "lineno": 1564,
    "metrics": {
      "cyclomatic": 17,
      "nesting": 5
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor Re-baselined 2026-08-20 from cyclomatic=16->17 (+1) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "api_viz_search",
    "lineno": 1697,
    "metrics": {
      "cyclomatic": 18,
      "nesting": 7
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/tools/memorize.py",
    "function": "memorize",
    "lineno": 68,
    "metrics": {
      "params": 11
    },
    "rationale": "PERMANENT. The 10 parameters are the public MCP tool schema the LLM calls directly; bundling into a dataclass would break the tool API and require a coordinated schema change across all callers. Re-baselined 2026-08-20 from params=10->11 (+1) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/tools/project.py",
    "function": "_render_project_brief",
    "lineno": 213,
    "metrics": {
      "cyclomatic": 37
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor Re-baselined 2026-08-20 from cyclomatic=35->37 (+2) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/tools/recall.py",
    "function": "recall",
    "lineno": 111,
    "metrics": {
      "fn_loc": 225,
      "params": 10
    },
    "rationale": "PERMANENT. MCP tool entry point. Phase 2a: forward-only rewrite reduced fn_loc 436->180. ADR-0215 (branch removal) then deleted the branch-detection block, dropping cyclomatic below the HARD cap \u2014 that metric is gone from this entry. ADR-0215 Car 5 then dropped the branch_hint param (10->9). fn_loc remains above the cap due to multi-path input validation (type_filter, mode, profile, consolidation, shadow observe). params=9: all 9 are required by the MCP schema \u2014 splitting into a dataclass would break the API surface. Allowlisted permanently; revisit when Phase 2b extracts validation helpers. Car 7 (2026-08-13) resynced fn_loc 180 -> 208 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. Re-baselined 2026-08-20 from fn_loc=208->221 (+13), params=9->10 (+1) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-20 on the signals-that-lie train merge: fn_loc 221 -> 225 (+4). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/tools/wiki.py",
    "function": "wiki_add",
    "lineno": 351,
    "metrics": {
      "cyclomatic": 25,
      "params": 16
    },
    "rationale": "PERMANENT. Public MCP tool: the params are the LLM-facing schema and cannot be bundled without breaking the API. Car C (#83) adds slug+upsert (2 params). ADR-0215 Car 5 dropped branch+branch_hint, leaving 14. R3 Car 1: sync write moved to backend run_wiki_add_replay; wait path (_wiki_add_wait_path) is a payload-dict helper. Residual cyclo is essential orchestration glue across enqueue, wait polling, validation, approval gating, and error normalization. Re-baselined 2026-08-20 from params=14->15 (+1) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-21, car/wiki-truncation-guard (ledger task 271): params -> 16 for ``allow_truncation``, keyword-only. The PERMANENT rationale above applies unchanged -- the params ARE the LLM-facing schema and cannot be bundled without breaking the API, which is exactly why this flag has to be one: a size-collapse refusal that names an escape hatch no MCP caller can pass would leave the operator with no route forward. It is deliberately NOT folded into the existing ``force``: that flag bypasses duplicate detection on CREATE, and reusing it would silently open the truncation gate for every dedup bypass.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/_shared/storage/migrations.py",
    "function": "_migration_016_directory_context",
    "lineno": 528,
    "metrics": {
      "nesting": 5
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/_shared/storage/migrations.py",
    "function": "_migration_018_directory_context_backfill_repair",
    "lineno": 653,
    "metrics": {
      "nesting": 5
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/_shared/config/config_yaml.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 2220
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor. v5.89 #67: +~490 LoC of FIELD_META schema data (desc/section for 67 backfilled knobs draining the I25 Tier-2 grandfathered backlog) \u2014 pure data, not logic; the module is a single-responsibility config loader by design. Car 7 (2026-08-13) resynced file_loc 1923 -> 2219 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. | Re-baselined 2026-08-20 on the signals-that-lie train merge: file_loc 2219 -> 2220 (+1). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/http.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 3620
    },
    "rationale": "pre-existing large file; updated from 1842 in v5.100.0 after three hook endpoints received shadow-observe instrumentation blocks (~90 LoC) plus accumulated growth from v5.97-v5.100 hook additions. Re-baselined 2284->2748: the bulk is accumulated growth since v5.100.0; #30 added ~44 LoC (two _record_subagent_* metric helpers + the subagent-capture wiring). Car B (2026-08-14 train) re-baselined 2748 -> 3300 (+552, +20%): the new ``_register_session_binding`` / ``lookup_session_binding`` / ``_clear_session_bindings`` / ``_register_session_bind_route`` quartet (~160 LOC of code+docstring+@trace_span) plus the per-request session-binding middleware (~280 LOC: ASGI dispatch, ContextVar setup, lookup-with-fallback) plus the project_id-decode fallback when the ASGI scope doesn't carry the session nonce (~80 LOC) plus the integration with /mcp and /sse routes that need their existing handlers wrapped. None of this can be split into siblings without restructuring the request pipeline (the binding lookup has to run BEFORE the route handler resolves scope-bound state). Scheduled for wave N split alongside the existing ``yadgar/_shared/runtime/session_bind.py`` and ``session_project.py`` siblings once Car D's re-key lands \u2014 Car D's new project_id-based resolution means most of the session-binding plumbing is project_id-only, and the wave-N split can put the ASGI middleware in a sibling then. Car 4 (2026-08-20 train, complexity-allowlist-ratchet) re-baselined 3300 -> 3496 (+196, +5.9%): this car turns the HARD allowlist gate from an unconditional pass into a ratchet (is_allowlisted() now requires actual <= recorded, not just presence in the allowlist) -- the previous 3300 entry was silently 196 lines stale and the old gate never noticed. 3496 is the measured LOC as of this car. Car 1 of the same train is expected to add further lines to this file; if it pushes past 3496, that car must re-baseline again with its own rationale rather than relying on this number. | Re-baselined 2026-08-20 on the signals-that-lie train merge: file_loc 3496 -> 3564 (+68). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size. | Car 20 (ledger task 303, same train) re-baselined 3564 -> 3610 (+46, +1.3%): one new sibling helper, ``_observe_dropped_actions``, called from BOTH of ``hook_auto_capture``'s drop branches. It cannot move to a sibling module without taking ``_split_batch_by_project`` with it (the two are one decision -- what to discard, and how loudly to say so), and that pair is only meaningful beside the handler that flushes the batch; a wave-N split should move all three together. The +46 is mostly rationale: the helper exists because the one-line ``logger.debug`` it replaces is precisely what let action_log capture die for six days behind HTTP 200s, so the docstring records why the drop is now WARNING+metric and why the response deliberately stays 200. Already trimmed once before re-baselining -- the long-form narrative lives in yadgar/tests/server/test_auto_capture_drop_audible.py's module docstring instead. | Re-baselined 2026-08-22 (C4 task 67 car): file_loc 3610 -> 3620 (+10, +0.3%). Growth is the ``_apply_readiness_antiflap`` helper extracted from ``_build_health_payload`` so the anti-flap counter, ``status``, and ``db``/``embed`` fields all stamp from one snapshot; the helper needs its own docstring recording why the two-step gate that previously produced ``embed:false`` + ``status:ok`` simultaneously was wrong. The two other probe-related helpers (session count, db+embed concurrent gather) stayed inlined -- extracting them would have added more lines (3 wrappers + 3 docstrings) than they saved, and the I33 rule would have flagged them for missing tri-signal coverage. The +10 is almost entirely the docstring (it documents WHY one snapshot, not just what the helper does) plus the helper's ``@observe(tier=\"stage\")`` decoration. Documented so the ratchet catches future drift; NOT a review-and-approve of the size.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/tools/project.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 2723
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor. Grew with the ADR-0007 agent-prompt capture nudge (_apply_agent_prompt_signal + _get_agent_prompt_toc_updated_at), which mirrors the existing ADR signal helpers in the same module \u2014 keeping them co-located is the established pattern; refactor remains deferred to the wave N split. Re-baselined 2026-08-20 from file_loc=2660->2716 (+56) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Car E1 (task 310, dc-eradication train) re-baselined 2716 -> 2723 (+7): _check_session_end_sentinel's SQL predicate was renamed from directory_context to project_id (the task's actual fix), and its docstring was expanded to record the live corpus counts that justified the change and why the other 30 directory_context refs in this file were left untouched. No new function; the delta is docstring + a one-line SQL text change.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/core/server/tools/wiki.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 2251
    },
    "rationale": "pre-existing; v5.61.0 +412 LOC for L2+L3 MCP tool registrations (wiki_replace_at/delete_at/insert_at/replace_markdown_block + heading_type param for wiki_append_section); scheduled for v5.55 wave N refactor. Car 7 (2026-08-13) resynced file_loc 1579 -> 1876 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. Re-baselined 2026-08-20 from file_loc=1876->2088 (+212) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-20 on the signals-that-lie train merge: file_loc 2088 -> 2125 (+37). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size. | Re-baselined 2026-08-21, car/refusal-not-crash (ledger tasks 80 + 294): file_loc 2125 -> 2130 (+5). wiki_delete is the ONLY wiki tool that post-processes _forward_admin's result rather than returning it verbatim, so it is the only one that has to recognise the new structured-refusal envelope; without the +5 it reports Car J's mutability lock as \"Wiki page not found\", which trades the old bare HTTP 500 for a wrong answer -- exactly the class of defect this train exists to remove. Correctness fix, not feature growth; NOT a review-and-approve of the size, and decomposition of this module remains the separate wave-N follow-up named above. | Re-baselined 2026-08-21, car/wiki-truncation-guard (ledger task 271): file_loc -> 2142 (+12 over the pre-car value). ``wiki_add`` gains ``allow_truncation`` -- the operator-facing half of the size-collapse gate, which refuses an update whose new body is under half the old one. The refusal message NAMES this flag, so without it the gate would be a dead end for the exact caller it is aimed at, which is the same defect the guard exists to remove. The +12 is one parameter, one payload key, and the eight docstring lines that keep it distinguishable from ``force`` (which bypasses duplicate detection on CREATE and must not open this gate too). Already trimmed once, from +15. The gate's logic and its measured threshold live in ``_shared/storage/truncation_gate.py``, not here. NOT a review-and-approve of the size; the wave-N split named above remains the honest fix. | Re-baselined 2026-08-22, train/C0 (wiki_add(project=...) satisfies the directory gate): file_loc 2142 -> 2187 (+45). The +45 is the gate helper's docstring block recording WHY the gate resolves-before-rejects (Car 5 registry invariant + C13 envelope shape) + the resolve branch itself + the call site's two-line unpacking and if/else that reuses the resolved id to skip a redundant resolver pass. The +45 is essential-feature surface: the override must reach the resolver even when the MCP transport cannot supply directory=, and the prose is the thing that stops the next reader deleting the resolve-before-reject branch as redundant. NOT a review-and-approve of the size, and the wave-N split named above remains the honest fix. | Re-baselined 2026-08-22, train/C9 task 70 (wiki_read content cap): file_loc 2187 -> 2223 (+36 over pre-C9 baseline). The +36 is the shared ``_WIKI_READ_CONTENT_CAP_BYTES = 8_192`` constant plus its 8-line lockstep-rationale docstring (paired with task 71; task 71's WRITE-side block was added in a separate car to keep the diff per-commit focused), plus the read-side cap block (14 lines: byte-length compare, char slice, the two markers, the cache-put ordering comment). The constant is essential correctness surface, not feature growth: a 50 KB rollup would drag 50 KB over the MCP boundary on every read. Task 71 will re-baseline again with its own +delta. Recorded so the ratchet catches FUTURE drift; the wave-N split named above remains the honest fix. The +36 over the prior 2187 is the measured file_loc at THIS car; after task 71 lands the file will grow by an additional ~17 LOC and the next car must re-baseline again. | Re-baselined 2026-08-22, train/C9 task 71 (wiki_append_section content cap): file_loc 2223 -> 2240 (+17). The +17 is the write-side cap block on ``wiki_append_section`` (the 13-line refusal-envelope if/return plus 4-line lockstep-rationale docstring on byte-vs-char + paired-with-task-70 movement). Essential correctness surface: a 50 KB append grows the row past the read cap (task 70) so the truncation marker would fire on EVERY subsequent read -- the append succeeded but the page was made unreadable. Paired with task 70 on the SAME constant so the two sides cannot drift. Recorded so the ratchet catches FUTURE drift; the wave-N split named above remains the honest fix. | Re-baselined 2026-08-22, train/C9 task 223 (wiki_delete broadened refusal guard): file_loc 2240 -> 2245 (+5). The +5 is one widened condition (``refused`` OR ``reason``) on the post-processing guard at line 1076 of tools/wiki.py, plus a 7-line comment block explaining WHY the widening is safe against the live envelope shape (live contract keys on ``refused=True`` from forward.py:115-120; the ``or _res.get(\"reason\")`` arm is defence-in-depth so a future envelope variant dropping the marker does not silently re-open the bare-500 defect). Essential correctness surface: ``wiki_delete`` is the ONLY wiki tool that post-processes ``_forward_admin``'s result rather than returning it verbatim, and the prior narrow guard would mis-classify a locked-page refusal as ``\"Wiki page not found\"`` (the OLD 500 swapped for a fresh lie). Same error-surface class as PR #54's wiki_set_mutability fix and car/refusal-not-crash above; this car closes the parallel on wiki_delete. NOT a review-and-approve of the size; the wave-N split named above remains the honest fix. |  Re-baselined 2026-08-23, train/C11 (PR #65 review finding #2 byte-slice cap): file_loc 2245 -> 2251 (+6). The +6 is the byte-encoded `_total` assignment plus the byte-slice + `errors=\"ignore\"` decode, replacing the broken char slice on the same C9 cap block. The +6 is essential correctness surface: the C9 cap guarded on `len(content.encode(\"utf-8\"))` but sliced on `content[:cap]`, leaking 3x the promised bytes for CJK pages. Car C11 makes the contract symmetric (guard AND slice on bytes). Folds finding #8 too (double `.encode(\"utf-8\")` on the over-cap cache-miss path now encodes once into `_total`). Recorded so the ratchet catches FUTURE drift; the wave-N split named above remains the honest fix.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/_shared/storage/migrations.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 2133
    },
    "rationale": "pre-existing; scheduled for v5.55 wave N refactor. v5.157.0 #76: recorded 1153 had drifted to ~1369 from migrations accumulated since 2026-06-13 (this touch only adds migration 026_drop_wiki_draft, ~+18 LoC net after removing the wiki_draft _init_schema entry); resyncing the recorded value. Each migration is an append-only single-responsibility fn \u2014 the module is a migration ledger by design; wave N split remains deferred. ADR-0215 Car 9: 1387 -> 1738 (+351). Migration 029 is unusually large for a ledger entry because it is a DATA migration, not a DDL one-liner: it carries five helpers (_m029_count / _m029_purge_unprotected_branch_memories / _m029_collision_pairs / _m029_resolve_slug_collisions / _m029_null_branch / _m029_assert_branch_fully_nulled), a Migration029Abort exception type and four measured constants, each abort path carrying the operator-facing message that tells a deploy-time reader whether to restore from the backup. Split into helpers rather than one function precisely to keep per-function complexity inside the caps; the cost lands on file_loc. Wave N split of this module remains deferred. Car 7 (2026-08-13) resynced file_loc 1738 -> 2076 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. | Re-baselined 2026-08-21 (car/stamp-project-id, ledger task 309): file_loc 2076 -> 2133 (+57). The whole delta is migration 034 \u2014 one DDL append declaring project_id + an index on entity / relationship / memory_cluster, the three graph tables C11's directory-bearing criterion never reached. It is the same single-responsibility append-only ledger entry every migration here is, and it is deliberately SCHEMA-ONLY (no data step), so the length is its module-level tuple and the docstring recording why the backfill lives in an operator-invoked admin op instead. Recorded so the ratchet measures FUTURE drift from the truth; NOT a review-and-approve of the file's size. The wave-N split of this module remains deferred.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/_shared/wiki/store.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 2876
    },
    "rationale": "pre-existing large file; v5.61.0 +281 LOC for edit primitives (L4+L1) + ~484 LOC for L2+L3 (positional/structural primitives + helpers); scheduled for refactor post-v5.64. Updated v5.85 S3: +81 LOC for tag-aware recall helpers (_collect_wiki_vector_scores_tagged, _collect_scores_dispatch, _tag_filter_pass extended). T2 Car C: file relocated wiki.py -> wiki/store.py (contract split; internal I13 split remains task #18). Car 7 (2026-08-13) resynced file_loc 2251 -> 2661 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. Re-baselined 2026-08-20 from file_loc=2661->2800 (+139) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-21, car/wiki-truncation-guard (ledger task 271): file_loc 2800 -> 2812 (+12). The +12 is the size-collapse gate's two WikiStore seams and their notes: ``add``'s upsert branch forwards the caller's ``allow_truncation`` to ``storage.update_wiki_page``, and ``restore_version`` passes ``_allow_truncation=True`` because restore is the RECOVERY path the refusal message names as the undo. The gate's LOGIC is not here at all -- it went to the new sibling module ``_shared/storage/truncation_gate.py``, the same extraction ``mutability_gate.py`` got from this file's neighbour for this same cap. What lands here is irreducible call-site wiring: each seam is one kwarg plus the note that stops a later reader deleting it as redundant. The canonical-seam exemption was deliberately NOT written here -- it lives in ``run_wiki_add_replay``, which is the only thing that knows a write is canonical, which is also why ``add``'s cyclomatic is unchanged at 16. NOT a review-and-approve of the size; the wave-N decomposition named above remains the honest fix.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "scripts/check_complexity.py",
    "function": "_check_function",
    "lineno": 279,
    "metrics": {
      "cyclomatic": 26
    },
    "rationale": "PERMANENT. Central dispatch for function-level complexity checks; each branch handles one metric (cyclo/loc/nesting/params). Splitting would scatter the check logic with no single place to see all enforcement rules. Self-referential: the checker itself cannot be split without undermining the policy it enforces.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "scripts/check_complexity.py",
    "function": "_check_class",
    "lineno": 422,
    "metrics": {
      "cyclomatic": 21
    },
    "rationale": "PERMANENT. Central dispatch for class-level complexity checks (methods, attrs, inheritance depth). Mirrors _check_function structure; splitting would break the parallel check pattern without improving clarity. Self-referential: the checker itself cannot be split without undermining the policy it enforces.",
    "added": "2026-06-13",
    "added_by": "complexity-governance-agent"
  },
  {
    "path": "yadgar/tests/server/test_integration.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1104
    },
    "rationale": "pre-existing large integration test file; not caused by this refactor",
    "added": "2026-06-14",
    "added_by": "refactor/v5.55-curate-params"
  },
  {
    "path": "yadgar/tests/server/test_integration.py",
    "function": "test_clean_startup_and_shutdown",
    "lineno": 999,
    "metrics": {
      "cyclomatic": 28
    },
    "rationale": "pre-existing complex integration test; not caused by this refactor",
    "added": "2026-06-14",
    "added_by": "refactor/v5.55-curate-params"
  },
  {
    "path": "yadgar/_shared/storage/memory.py",
    "function": "_enrich_memory_if_enabled",
    "lineno": 202,
    "metrics": {
      "cyclomatic": 20
    },
    "rationale": "PERMANENT. v5.55 extract from insert_memory: 5-term enable guard + 6-field result mapping + set_parts loop/conditional re-encode; further splitting degrades locality without reducing branch count \u2014 cohesive enrichment pipeline, single responsibility.",
    "added": "2026-06-14",
    "added_by": "insert-memory-refactor"
  },
  {
    "path": "yadgar/_shared/storage/memory.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1680
    },
    "rationale": "RE-BASELINED 2026-08-21 (car/memory-update-bypass, ledger task 296) from 1651 -> 1680 (+29). The file sat at EXACTLY its recorded ceiling, so ANY net addition trips the gate. The addition is the task-296 floor: `content` has four writers (memory_update, the nightly compressor, the conflict-resolver UPDATE op, curation's merge) and only the first re-synced the six enrichment columns, so the other three left `enriched_content` and its siblings describing text the row no longer holds. `update_memory_fields` is the one funnel all four already pass through, which is why the null-out lands in THIS file rather than at three call sites that a fourth caller would silently join. The code itself is 5 lines (a pre-rendered clause tuple + a one-`return` helper + a one-line `set_parts.extend` at the call site); the other 24 are the helper docstring recording WHY the null-out belongs in the funnel while the re-derivation cannot (a caller writing its own embedding after this call would clobber an enrichment-derived vector written here, and curation's merge embeds `contextual_prefix + content`, which storage cannot compute). That prose is the thing that stops the next reader 'simplifying' the split back into the single seam this car refuted. It was already compressed once \u2014 the inline call-site comment was folded into the helper docstring rather than kept in both places, which is why +29 and not +43. The helper is extracted rather than inlined solely because inlining put `update_memory_fields` at cyclomatic 17 against the I30 HARD limit of 15. Shedding the remaining 29 would mean deleting unrelated prose from a 1680-line file on a bug-fix branch: churn with no readability gain, outside this car's remit. The honest fix remains the _MemoryMixin split named below, still tracked separately. Prior rationale: Storage _MemoryMixin; file drifted to 1204 across v5.9x perf cars (recorded baseline 1027 was stale). v5.104 PIECE D adds find_memory_ids_by_entities (+37 LOC) \u2014 the batched read-side of the spreading-activation N+1 fix, exact-parity with the per-name loop it degrades to. A new mixin extraction would break the class_methods count; the storage split is a larger refactor tracked separately. RE-REVIEWED 2026-08-13 (car3/unscoped-reads): the recorded 1241 was ALREADY stale by 246 lines \u2014 the file stood at 1487 before this car, i.e. +19.8%, parked just under the 20% tolerance where the gate could not see it. This car adds +39 (1487 -> 1526) and is what pushed it into view: get_recent_memories and get_stale_memories each gain an optional project_id arm plus the note recording why the predicate is directory_context and not the project_id column \u2014 both were corpus-wide reads that served every project's rows through restore and the memory://stale MCP resource. The +39 is fixing a cross-project leak, not a feature, and the honest remedy for the file remains the _MemoryMixin split tracked with its storage/wiki.py and storage/migrations.py siblings. The number is corrected here rather than left drifting so the next +20% is measured from the truth. Re-baselined 2026-08-20 from file_loc=1526->1527 (+1) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-20 on the signals-that-lie train merge: file_loc 1527 -> 1651 (+124). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size.",
    "added": "2026-06-25",
    "added_by": "feat/v5.104-perf-train",
    "reviewed": "2026-08-21",
    "reviewed_by": "car/memory-update-bypass"
  },
  {
    "path": "yadgar/core/server/tools/adr_render.py",
    "function": "_build_adr_body",
    "lineno": 46,
    "metrics": {
      "params": 11
    },
    "rationale": "PERMANENT. ADR schema has 10 mandatory content fields (title, status, date, context, decision, rationale, alternatives, consequences, revisit_trigger, supersedes) plus adr_id. Each field maps to a distinct semantic slot in the ADR template; collapsing into a dict or dataclass would break the FastMCP tool signature (which must be flat keyword args for schema generation). The 11-param limit is inherent to the ADR structure, not accidental complexity.",
    "added": "2026-06-25",
    "added_by": "feat/improvement-train"
  },
  {
    "path": "yadgar/core/server/tools/adr.py",
    "function": "adr_add",
    "lineno": 182,
    "metrics": {
      "params": 14
    },
    "rationale": "PERMANENT. Same ADR-schema constraint as _build_adr_body: 10 mandatory content fields + directory = 11 total. FastMCP derives the JSON Schema from function parameters directly; a **kwargs variant loses schema enforcement. The 11-field ADR structure is a conscious design choice (matches Nygard ADR format) and must be preserved as a flat tool signature. Car M (0047 spine train, 2026-08-09): params 11 -> 12 for the cross-project project= override (keyword-only param). Car H (0047 spine train, 2026-08-09): params 12 -> 14 for tier + subsystem fields (D27 tier enum + D28 subsystem explicit), and fn_loc 11 fields moved the function body past the 150 HARD cap to 155 (C5, 0047 PR#40 \u00a75: the fn_loc entry is REMOVED \u2014 deleting the directory-derivation tier collapsed adr_add's two-pass project_id resolve into one, bringing the body back under the cap; params stays 14); the per-row stamp + tier/subsystem rollup trigger live below the entry point in extracted helpers (_trigger_subsystem_rollup_regen) so the entry point remains the single flat FastMCP-visible surface.",
    "added": "2026-06-25",
    "added_by": "feat/improvement-train"
  },
  {
    "path": "yadgar/core/server/tools/task.py",
    "function": "task_write",
    "lineno": 221,
    "metrics": {
      "params": 13
    },
    "rationale": "Car D (0047 spine train \u00a77 row D): task_write mirrors the task-table column set (project_id, title, status, state, active_form, plan_path, body_slug, id, blocked_by, blocks \u2014 10 params, ADR-0197/\u00a714.1). FastMCP derives the JSON Schema from function parameters directly; a **kwargs variant loses schema enforcement on the harness side. The D39 blocked_by/blocks list + D36 nullable state + D12 title cap + \u00a716.10 completion-clears-state all ride this one signature by design \u2014 the four feature surfaces would otherwise scatter across four near-identical tools. Validation, payload assembly and the create/update dispatch are already extracted into _validate_write_inputs / _build_create_payload / _build_update_payload (see module docstring); what remains here is the entry-point shape and the create-vs-update dispatch. Car M (0047 spine train, 2026-08-09): params 10 -> 11 for the cross-project project= override (keyword-only param). Cyclomatic and fn_loc unchanged (the override resolution is two inline `try/except` blocks + one helper call). Car C part 2 (2026-08-16): params 11 -> 13 for clear_plan_path / clear_body_slug (both keyword-only, default False). They exist because None on plan_path/body_slug already means \"the caller did not mention this column\" and '' is a real value that collides on uq_task_project_body_slug \u2014 so neither existing spelling can express \"clear this column to NULL\", and no re-typing of the existing parameters recovers the third state. The alternative considered and rejected was a pair of single-field setter tools (the wiki_set_metadata shape): it trades 2 params here for 2 more entries on an MCP surface that ledger task 76 already wants classified for being too large. Cyclomatic is unchanged \u2014 the flags are one elif each in _build_update_payload and one predicate helper (_validate_clear_flags) that task_write calls once.",
    "added": "2026-08-09",
    "added_by": "car/D-task-tools"
  },
  {
    "path": "yadgar/_shared/observability/metrics.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1222
    },
    "rationale": "#80 daemon-obs: +~125 LoC of cohesive Prometheus collectors for the loop-freeze RCA (yadgar_event_loop_lag_seconds histogram + _max gauge, yadgar_tool_pool_* gauges) plus their refreshers (loop-lag probe monitor, _collect_pool_stats). This module is a single-responsibility metric registry \u2014 every metric in the daemon is declared here against the shared _registry, so the gauges belong co-located with their siblings. Splitting the registry would fan the _registry import across modules without reducing essential complexity. Scheduled for the wave N module split. Car 7 (2026-08-13) resynced file_loc 1059 -> 1222 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size.",
    "added": "2026-07-01",
    "added_by": "fix/daemon-obs-gauges"
  },
  {
    "path": "yadgar/_shared/config/config.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1203
    },
    "rationale": "P5 obs-wave: +6 LoC of @observe decorators on _load, get_field_value, _validate_hhmm, _validate_log_format, resolve_knob pushed file from 1000 to 1006. Single-responsibility pydantic Settings module; all fields and validators belong co-located. Scheduled for wave N settings-split. Car 7 (2026-08-13) resynced file_loc 1006 -> 1182 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size. | Re-baselined 2026-08-20 on the signals-that-lie train merge: file_loc 1182 -> 1203 (+21). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size.",
    "added": "2026-07-03",
    "added_by": "feat/obs-p1-recall-readpath"
  },
  {
    "path": "yadgar/core/server/tools/recall.py",
    "function": "_forward_to_backend",
    "lineno": 44,
    "metrics": {
      "params": 12
    },
    "rationale": "Phase 2a: pure forwarder function \u2014 mirrors the recall() MCP signature plus the transport params (timeout_s, deadline_ms) the backend call needs. Decomposing into a dataclass would add a dataclass definition just to pass 10 fields to one call site \u2014 not worth it. All params are directly forwarded to the backend payload; same 'bundled MCP schema' rationale as recall() itself. Allowlisted: Phase 2a, #45. Car 7 (2026-08-13) resynced the recorded value 10 -> 12 (measured drift, under the 20% gate); the underlying justification was not re-audited at the larger size.",
    "added": "2026-07-05",
    "added_by": "phase2a-cutover-agent"
  },
  {
    "path": "yadgar/backend/cache/cache.py",
    "function": "__init__",
    "lineno": 340,
    "metrics": {
      "params": 9
    },
    "rationale": "Backend caching train Car 0 (#49): the unified backend Cache constructor mirrors the core yadgar/cache.py Cache (which sits at the 8-param cap: name/max_bytes/invalidation/key_fn/deep_copy/obs_tier/clock) plus one backend-only param, checkpoint_hash, needed to ckpt-gate the msgpack snapshot (a snapshot written under a different model hash must be discarded on load \u2014 the ce/embed model-swap contract). Each param is an independent, orthogonal cache policy bound once at construction; wrapping them in a config-object would add a dataclass indirection for a single construction site (the two _make_*_cache factories) with no readability gain. Allowlisted: Car 0, #49.",
    "added": "2026-07-06",
    "added_by": "backend-cache-train-car0"
  },
  {
    "path": "yadgar/_shared/wiki/store.py",
    "function": "add",
    "lineno": 670,
    "metrics": {
      "cyclomatic": 16
    },
    "rationale": "Car F (0047 \u00a77): WikiStore.add gained one +1 cyclo branch (the sanctioned token thread-through) for the Car J mutability gate, pushing the pre-Car-F 15 to 16. The branch is irreducible: storage.insert_wiki_page reads _sanctioned from the page dict, so the call site can't be moved out. Bundling the new branch into the existing policy+slug-resolution preamble wouldn't reduce cyclomatic \u2014 it would just relocate one branch. Decomposing add() into a per-page-type helper family is a wiki-store complexity wave of its own (the function already has 3 nested helpers: _resolve_slug, _merge_existing_tags, _build_new_page).",
    "added": "2026-08-09",
    "added_by": "car-f-adr-tools"
  },
  {
    "path": "yadgar/core/server/tools/misc.py",
    "function": "checkpoint",
    "lineno": 96,
    "metrics": {
      "params": 9
    },
    "rationale": "C3 (0047 PR#40 remediation \u00a75.C3) adds `project` to all 42 scoped MCP tools; checkpoint was the only one already sitting exactly ON the params_hard=8 cap, so it lands at 9. The parameter is not optional-by-taste: the AST surface test pins every @_tool function taking `directory` to also take `project`, and checkpoint is one of them. Deliberately NOT bundled into an options dataclass (the memorize/wiki_add I13 remedy) because C5 REMOVES `directory` from this same signature, returning it to 8 without a refactor \u2014 bundling now would be churn in both directions across one PR. Delete this entry in C5.",
    "added": "2026-08-10",
    "added_by": "car-c3-project-param"
  },
  {
    "path": "yadgar/backend/write_exec/memorize_impl.py",
    "function": "run_memorize_replay",
    "lineno": 32,
    "metrics": {
      "params": 9
    },
    "rationale": "C4b (0047 PR#40 \u00a75): params 8 -> 9 for the enqueue-time project_id. This function deliberately MIRRORS the memorize MCP signature (it is the drain-replay half of the same call) and already carried # noqa: PLR0913 for that reason; every one of its parameters is a field of the queue payload, so the count tracks the payload's width, not branching (cyclomatic and nesting are unchanged). Bundling the nine into a dataclass is the obvious reduction and is explicitly NOT taken here: the one-to-one correspondence with the payload keys is what makes a missing field visible at the call site in apply.py, and C5 is about to make a missing project_id fail loud there. The bundling refactor belongs with C5, which changes that contract anyway. Same deferral shape as Car M's wiki_add entry above.",
    "added": "2026-08-11",
    "added_by": "car-c4b-memorize-stamp"
  },
  {
    "path": "yadgar/_shared/storage/user.py",
    "function": "insert_profile",
    "lineno": 98,
    "metrics": {
      "params": 9
    },
    "rationale": "Car C13f (0047 PR#40 \u00a75): params 8 -> 9 for project_id. The function was sitting EXACTLY on the HARD cap (8 incl. self), and the added parameter is essential feature surface \u2014 one more user_profile column \u2014 not logic branching: cyclomatic, nesting and fn_loc are all unchanged. It cannot be dropped or defaulted, because it is the scope key the read path now filters on (search_profiles_fts pushes 'AND project_id = $pid'), and ADR-0227 forbids deriving an identity in-process, so the value has to arrive from the caller. Keyword-only does not help: complexity_audit.param_count sums args + kwonlyargs + posonlyargs, so the count is identical. THE HONEST FIX IS A ProfileRecord DATACLASS \u2014 precisely the refactor the SIBLING function already had done to it: insert_belief takes a single BeliefRecord whose docstring says its fields 'map 1-to-1 to the derived_belief table columns', which is what collapsed that signature. Mirroring it here would take insert_profile to 3 params and retire this entry outright. It is deferred because it is a storage-API change with a blast radius of ~17 insert_profile call sites (yadgar/tests/_shared/test_bitemporal_extension.py alone holds 16), and this car is the last blocker on the PR's make-e2e push gate \u2014 a broad signature churn here buys nothing for the three production bugs the car exists to fix and risks the gate it exists to turn green. Same deferral shape as the wiki_add Car M entry above (essential parameter surface, extraction owned by a dedicated refactor).",
    "added": "2026-08-12",
    "added_by": "car-c13f-classb-fixes"
  },
  {
    "path": "yadgar/_shared/storage/wiki.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1106
    },
    "rationale": "Car F1 (PR #40 CI failure class B): this file sat at EXACTLY 1000 \u2014 the HARD cap itself \u2014 before this change, so ANY net addition trips the gate. The addition is +17 net for a crash fix, not a feature: SurrealDB NONE and NULL are different values and each emptiness guard ADMITS the other's, so the two brute-force cosine arms here (search_wiki_vectors scoped, search_wiki_vectors_tagged) took a second guard, and the two writers here (insert_wiki_page, update_wiki_page) stopped binding a Python None that lands as SurrealDB NULL over the HTTP transport and then kills every scoped recall with 'Expected `array<number>` but found `NULL`'. Nine of the 17 lines are pure code (the emb_assign branch, the update_wiki_page embedding branch); the rest are the docstring notes that stop the second guard being deleted later as redundant-looking belt-and-braces \u2014 which is exactly how the single-guard version shipped in C7. Those comments were ALREADY compressed once to get from +32 to +17, with the full account kept in storage/vector.py::search_vectors (the canonical site) rather than duplicated here. Shedding the remaining 17 would mean deleting unrelated prose from a 1000-line file on a CI-failure branch: churn with no readability gain, and outside this car's remit. The honest fix is the _WikiMixin split, which belongs to the wave N module split alongside its siblings (storage/memory.py, storage/migrations.py) already carrying entries above. Re-baselined 2026-08-20 from file_loc=1017->1068 (+51) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-21, car/wiki-truncation-guard (ledger task 271): file_loc 1068 -> 1088 (+20). ``_WikiMixin.update_wiki_page`` is the storage chokepoint Car J already chose for the mutability gate, and it is where ``compute_change_summary`` measures the size delta that the incident wrote down and shipped anyway. The +20 is the second gate call at that same chokepoint: one import, one ``_allow_truncation`` kwarg, the six-line ``enforce_no_size_collapse`` call, and the notes recording why the content is read from ``updates`` (so a metadata-only write compares the old body with itself and can never fire) and why the kwarg is NOT ``_sanctioned``. The gate's logic, its measured threshold and its refusal type all went to ``storage/truncation_gate.py`` -- the same split ``mutability_gate.py`` got out of this file for this cap. NOT a review-and-approve of the size; the _WikiMixin split named above remains the honest fix. | Re-baselined 2026-08-22, car/c9-bug-bag (ledger task 272): file_loc 1088 -> 1106 (+18). ``get_wiki_page_by_slug_directory`` (the directory-keyed lookup ladder Car W4 / Car M split off from the project-keyed twin) gained rung 3: a ``$reach IN tags`` SELECT that mirrors the rung the project-keyed ladder already has at ``get_wiki_page_by_slug_project:509-513``. The 18 lines are the rung-3 SQL block (two-query pattern, NOT one ``OR`` because LIMIT 1 would pick arbitrarily between a directory='global' row and a tag-reach row sharing the slug), the docstring update recording the four-rung shape, and the inline note explaining why the project-keyed ladder is the model. Without rung 3 every tool that falls through to the directory-keyed lookup (``wiki_append_section`` / ``wiki_restore`` / ``wiki_history`` / ``wiki_diff`` / ``wiki_read_version`` / every ``*_text`` / ``*_at`` / ``*_block`` tool, plus the four positional http_wiki_versioning endpoints) was structurally unable to find reach-tag-only rows like ``agent-prompt-pr-review`` -- the same defect class Car C7 fixed on the project-keyed side for ``wiki_read``. Mirror is required: a fix on one ladder but not the other re-creates the divergence this car closes. NOT a review-and-approve of the size; the _WikiMixin split remains the honest fix.",
    "added": "2026-08-12",
    "added_by": "car-f1-vector-null"
  },
  {
    "path": "yadgar/core/server/tools/project.py",
    "function": "_project_brief_signals",
    "lineno": 1725,
    "metrics": {
      "params": 9
    },
    "rationale": "Car F9 (0047 PR#40): the 9th parameter is `project_id`, the caller-supplied identity the session-end sentinel check needs. C10 (f) made `memorize` stamp `directory_context` from the resolved project_id, so the sentinel reader must be keyed on the project rather than the path; this helper sits between `project_brief` (which validates the override) and `_check_session_end_sentinel` (which consumes it), so the value has to travel through the signature. Deriving it here instead is exactly what ADR-0227 deletes. The obvious refactor \u2014 bundle the args into one dict, as the sibling `_project_brief_catalog_full` already does \u2014 is deliberately NOT taken in this car: C7 re-keys every remaining path-keyed query in `project_brief` onto project_id and will revisit this signature wholesale, and reshaping it twice would churn the same call site and its test callers for no interim gain. Every argument is a distinct signal input passed by keyword from the single call site; none is a flag controlling branches.",
    "added": "2026-08-12",
    "added_by": "car-f9-session-end-capture"
  },
  {
    "path": "yadgar/_shared/storage/sql/mariadb.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1279
    },
    "rationale": "Car B2 (2026-08-19 bug train) re-records the count at 1279. Ledger task 195 adds `list_adr_supersedes` \u2014 the reader for the `adr_supersedes` join table that `add_adr_supersedes` has written since Car F and that NOTHING has ever read, which is why every supersede-bearing ADR renders `supersedes: \"none\"` (22/22 across two corpora). D20 requires every ledger-row access to sit inside a method of a class literally named MariaStorageEngine, so the reader cannot go to the overflow home; only its non-SQL half could, and it has none (the fold is four lines over the result cursor). Ledger task 197's status->tier mapping DID go to `ledger_columns` (`adr_tier_for_flip`), leaving five lines at the `_flip_adr_status` call site. Prior rationale (Car B1): Car B1 (2026-08-19 bug train) re-records the count at 1212. Ledger task 188 adds the `project_id` scope arm to `get_adr_row` plus the rationale for why a by-id lookup over a GLOBAL AUTO_INCREMENT is wrong (~+22 LOC); ledger task 191's NULL-tier classification is delegated to `ledger_columns.adr_tier_where`, so only its 5-line call site lands here \u2014 i.e. the overflow home this entry's own conclusion names is where the new logic actually went. The recorded 1010 was already stale at 1183 before this car. Prior rationale (Car C, 2026-08-14 train): the bindparam fix adds +36 net LOC \u2014 list[Annotated, list] normalize branch + bindparam('status', expanding=True) wiring + the docstring note explaining empty-list = no-filter. The module was already at 996 LOC after the storage/memory.py split (it absorbed the task/ADR/agent_prompt/agent_discipline ledger writers when the dual-engine split moved), so even a single-line bindparam fix trips the 1000 HARD cap. The bulk of the file is the four single-table ledger writers (task/ADR/agent_prompt/agent_discipline) plus the cross-engine project registry mixin \u2014 both responsibilities are deliberate and not amenable to a wholesale split inside a one-car scope (the sibling `storage/memory.py` is the model for splitting ledger tables off, but doing it here would mean reshaping the project registry seam at the same time). Refactoring the file just to shed 10 lines on a bindparam fix branch is churn with no readability gain. The honest fix is the same split as `storage/memory.py`: task/ADR/agent_prompt/agent_discipline writers move to `storage/sql/task_ledger.py` et al., with MariaStorageEngine keeping the registry mixin. That's the same wave-N shape as the existing `storage/wiki.py` and `storage/migrations.py` entries above; not a Car-C carve.",
    "added": "2026-08-14",
    "added_by": "car-c-task-list-bindparam"
  },
  {
    "path": "yadgar/core/server/_app.py",
    "function": "_build_tool_wrappers",
    "lineno": 564,
    "metrics": {
      "cyclomatic": 18,
      "fn_loc": 195
    },
    "rationale": "Car B (2026-08-14 train): the function builds (sync, async) instrumented wrappers for every @_tool \u2014 each wrapper carries its own session-binding ContextVar plumbing (Car B's session_bind.bind_session entrypoint), and the per-tool branch count grew because Car B adds: (1) a ``sync_or_async`` discriminator (was implicit before Car B; now the function must decide which of the two wrappers to emit per tool, since Car B's offload-to-worker-thread offload hook runs INSIDE the wrapper and only one of the two wrappers needs it), (2) a ``needs_session_bind`` discriminator (tools that take an explicit ``project=`` override do NOT need the session-bind wrapper \u2014 only tools that read project from the ContextVar do), and (3) a ``forwards_to_backend`` discriminator (the proxy tools that already route through ``_forward_admin`` would double-wrap if they got the session-bind treatment). The net effect is +1 cyclomatic branch per discriminator + the existing branch tree, hitting 16 (limit 15); fn_loc grew 137 -> 162 with the discriminator branches and the docstring explaining why each discriminator exists. The honest fix is splitting into ``_build_sync_wrapper`` + ``_build_async_wrapper`` + a small ``_needs_*`` predicate set \u2014 but that move belongs to the wave-N module split alongside the existing Car 0037 entry below (and the F5/F10 / ci-local allowlists at the file level), not a Car B (binding-only) scope. Refactoring the wrapper inside a car whose entire purpose is to ADD session-binding (not refactor wrappers) would couple two changes and balloon the diff. Re-baselined 2026-08-20 from cyclomatic=16->18 (+2), fn_loc=162->180 (+18) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. | Re-baselined 2026-08-20 on the signals-that-lie train merge: fn_loc 180 -> 195 (+15). Growth is this train's own (maintenance envelope, registry-check-on-create, enrichment resync, prior rework). Recorded so the newly-ratcheting allowlist catches FUTURE drift; NOT a review-and-approve of the size.",
    "added": "2026-08-14",
    "added_by": "car-b-session-bind"
  },
  {
    "path": "yadgar/backend/embed_service/embed_service.py",
    "function": "<file>",
    "lineno": 1,
    "metrics": {
      "file_loc": 1007
    },
    "rationale": "Car F (task #61): this file sat at 1000 (the HARD cap) before this change, so ANY net addition trips the gate. The additions are +5 net: two new fields on the /health payload (``version`` and ``versions_compatible`` \u2014 the Car F version handshake), one lazy import inside the health handler so the import lives beside the function that uses it. The I13 docstring at line 9 already pins this module as an ACCEPTED single-file case (relocated to embed_service_handshake.py for the helpers themselves; only the dict field remains here). The honest fix is the embed_service module split the I13 docstring references; that restructure belongs to the parked T2 layer-boundary train, not Car F. Shedding 5 lines by removing the version_handshake or the lazy import would mean either inlining the bound check into the dict literal (loses the sibling-module boundary the I13 cap rationale demanded) or moving the import to module top (which is what was tried first and which ruff rejected on the function-grouping lint). The 5-line cost is the cheapest expression of the deploy-ordering fix that satisfies both the 1000 HARD cap gate AND the I13 single-file rationale. Re-baselined 2026-08-20 from file_loc=1005->1007 (+2) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up.",
    "added": "2026-08-14",
    "added_by": "car-f-deploy-ordering"
  },
  {
    "path": "yadgar/__main__.py",
    "function": "cli",
    "lineno": 59,
    "metrics": {
      "fn_loc": 169
    },
    "rationale": "Car D (2026-08-14 train): cli() was at fn_loc=150 (right at the HARD cap) before Car D. Adding migrate.register(subparsers) (the Car D wire-up of yadgar migrate rekey) plus adding migrate to the existing from-import block adds +1 net fn_loc, tripping the 150 HARD cap at 151. The fn is the single CLI dispatch surface; refactoring it into _register_subcommands / _handle_default_server / _dispatch_subcommand helpers would not reduce the cyclomatic (the dispatcher branches by subcommand either way) and would split the entry-point surface across modules \u2014 a carve that belongs to a separate refactor car, not to Car D whose scope is the corpus re-key migration. The 1-line cost is the cheapest expression of Car D wiring the subcommand exposes. Re-baselined 2026-08-20 from fn_loc=151->167 (+16) when the allowlist gate became a ratchet (car/complexity-allowlist-ratchet): the entry had been passing unconditionally because the recorded value was never compared to the current measurement, so it drifted silently. This re-baseline is NOT a review-and-approve of the new size on its own merits -- it records the as-measured value so the ratchet can catch every future change from here. Decomposition, if warranted, is tracked as a separate follow-up. Re-baselined 2026-08-21 from fn_loc=167->169 (+2) by the task-306 car: registering `yadgar verify-hooks` costs exactly two lines here -- the from-import name and the register(subparsers) call -- the same one-line-per-subcommand cost every entry in this dispatcher pays. The ratchet is doing its job by making that visible; the decomposition follow-up this rationale already names is unchanged and still not this car's scope.",
    "added": "2026-08-14",
    "added_by": "car-d-corpus-rekey"
  }
]

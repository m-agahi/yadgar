> ARCHIVED 2026-07-16 — shipped in this PR.

# Plan: viz-rest — remaining triage items (user verdict 2026-07-16)

Date: 2026-07-16 · Status: SHIPPED · Separate PR (NOT #208). Branch off master.

Source: `viz-triage-checklist-2026-06-27` code-verified — 60/90 already DONE.
This builds the user-confirmed remainder. Theme stays oscilloscope (no font work).
File:line hints from the verification pass; re-confirm before editing.

## Frontend-only items (index.html + graph JS + control.js)
1. **Nav reorg** — move `Stats` + `Health` nav entries from the System menu into the
   Observability nav-group (where Traces lives). `index.html` nav-group markup.
2. **Traces reload → blank (BUG)** — the SPA hash-router doesn't restore the Traces tab
   on reload. Register Traces in the tab-restore/hash-init path (like bookmarks/other
   tabs). Check `tabs.js` + the initial-tab restore in `index.html`. Repro first.
3. **#49 hold-click dim-panels bug** — no handler found; REPRODUCE first. If the bug is
   the dim-state getting stuck on hold-click, fix `_repaintDimState`/the hold handler.
   If not reproducible on current code → note it obviated, skip.
4. **#26 destructive-card CSS** — behavior (typed-name arm gate) exists (`control.js:634`);
   the `.destructive` / `.cfg-pending-destructive` CSS rules are absent → add red styling
   in the control-panel style block.
5. **#54 2D anchored-node shape** — 3D branches shape by `_anchor`→cube/wiki→octahedron
   (`index.html:2109`); the 2D canvas draw has no shape branch (size only). Add a 2D
   shape branch for anchored/wiki nodes.
6. **#29 config header status line** — `control.js` title shows knob count only; add
   version + pending-count + restart indicator to the panel header.
7. **#61 intra-match edge highlight** — `index.html:2173` only has the OR-dim branch;
   add an AND-bright branch (edge bright when BOTH endpoints match the search).
8. **#70 edge weight/threshold slider** — add a slider (beside the heat slider) that
   hides edges below a weight/count threshold. Payload already carries edge counts.
9. **#48 edge particles** — `linkDirectionalParticles` on directional edges
   (transition/memory_wiki/wiki_crossref) in the 3D ForceGraph config; keep arrows as
   the 2D fallback. Bounded particle count (perf).
10. **#13 cluster hulls** — draw convex hulls around clusters instead of only tinting
    member nodes (`_applyClusterColors`, `index.html:1992`). 3D: translucent hull mesh
    or 2D canvas hull. Colour by cluster; pairs with the galaxy idea later.

## Backend-touching items (payload) → backend_version bump
11. **#55 last_accessed per node** — add `last_accessed` to the memory node payload
    (`backend/graph/graph_nodes.py`) + show it in the detail panel (`graph-detail.js`).
12. **#89 weak-edge render toggle** — `_build_transition_edges` skips `count<2`
    (`graph_edges.py`); add a `?include_weak=1` param (or reuse a viz knob) + a frontend
    toggle to render them. Default OFF (current behavior preserved).
13. **#14 astrocyte cluster source** — VERIFY FIRST: is `astrocyte_domain` data actually
    populated? Grep the schema/table + a count path. If EMPTY → skip (no-op, note it).
    If populated → surface `astrocyte_domain` as a cluster source in
    `_build_clusters_payload` (`graph_edges.py:243`) alongside memory_cluster.

## Sequencing / conflict note
Branch `feat/viz-rest` off latest master. #208 (caps→0 + db_inspect) is open and touches
`graph_api.py` cap defaults + backend storage — LOW overlap with these payload additions,
but if #208 merges first, rebase. Prefer: let #208 merge, branch fresh; if building in
parallel, rebase onto master after #208 lands.

## Version + housekeeping
- Backend payload changes (#55/#89/#14) → bump `backend_version` in ALL THREE:
  `server.json` + `docker-compose.yml` + `yadgar/__init__.py::BACKEND_VERSION` (the drift
  trap). Core bump too (frontend/core changes). CHANGELOG `## [Unreleased]`.
- I33 `@observe` on any new backend fn; I32 CAPABILITY_REGISTRY for new payload fields /
  the weak-edge param. Update `viz-triage-checklist` wiki verdicts for the items built.
- No new `except (A,B):` without `# fmt: skip` (py314/ruff). Env for tests:
  `YADGAR_OTLP_ENDPOINT=''` + `HF_HOME` set; pytest via script file.

## Test
No browser harness (convention) — cover payload/logic changes (#55/#89/#14/#70 filter
logic) via unit tests + graph_api contract; frontend render is user smoke-check. Extend
`test_viz_fidelity_*` / `test_graph_api_contract`.

## Tests-first / risks
- #49 + #14 are VERIFY-FIRST (may be no-ops) — don't build dead work; note if obviated.
- #13 hulls + #48 particles are the render-heavy ones — keep bounded (perf on --cpus 1
  client is fine, but cap particle counts + hull vertex counts).

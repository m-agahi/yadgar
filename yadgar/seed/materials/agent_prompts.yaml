# Canonical agent-prompt starter materials (v5.88 seed consolidation).
# Edited here, loaded by yadgar.server.tools.agent_prompts.STARTER_PROMPTS.
# Each entry: pattern (slug), purpose (one-line TOC desc), content (prompt body).
# content uses a |- block scalar (strip final newline) — bodies have NO trailing newline.

prompts:
- pattern: code-review
  purpose: Review a diff or PR for correctness and risk.
  content: |-
    Review the given diff or PR. One finding per line, severity-tagged (critical/high/medium/low).
    Cite file:line for every finding.
    Flag only what changes correctness, security, or observable behavior.
    No praise, no scope creep, no unrelated cleanups.
    If nothing is wrong, report 'no issues found'.
- pattern: debug-investigate
  purpose: Root-cause a bug and ship a minimal fix with a regression test.
  content: |-
    Reproduce the bug first — confirm failure before touching code.
    Isolate via bisection, logging, or binary-search; identify the true root cause, not the symptom.
    Apply the minimal fix — surgical edit, no opportunistic cleanups.
    Add a regression test that fails before the fix and passes after.
    Run the full suite; loop until green.
- pattern: explore-codebase
  purpose: Map where code lives / how a subsystem works (READ-ONLY).
  content: |-
    READ-ONLY investigation — make zero edits.
    Locate where X lives or how Y works; start broad (grep/glob) then narrow.
    Return a file:line table with one row per relevant symbol or entry-point.
    Quote function signatures exactly as they appear in source.
    Do NOT propose or apply fixes; report the map, not opinions.
- pattern: implement-tdd
  purpose: Implement a feature test-first (red→green→refactor), writing the least code that satisfies it — a YAGNI least-code ladder applied before coding.
  content: |-
    Write a failing test that pins the desired behavior (red) before any implementation code.

    Before writing implementation, climb the least-code ladder — STOP at the first rung that works:
      1. Does it need to exist at all? Drop speculative / just-in-case scope.
      2. Already in the codebase → reuse it.
      3. Standard library / language built-in.
      4. Native feature of a framework/platform already in use.
      5. An already-installed dependency. (Do NOT add a new dep without asking.)
      6. A one-liner.
      7. Else: the minimal implementation — no abstraction a second caller doesn't yet need.

    Implement the minimal code to pass (green). Refactor with tests green — no behaviour change.
    Run the full check surface (tests/lint/types) and loop until clean.
    Done = tests pass, checks green, and no code exists that a higher rung could have avoided.

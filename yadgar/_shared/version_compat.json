{
  "$schema": "internal://yadgar/version-compat.schema.json",
  "_comment": "Car F (task #61). Bounds the core<->backend version window. The handshake in /health refuses to talk to a peer whose version is outside these ranges. Major.minor only — a patch release never breaks the wire format. Bump the *_max when the next wire-incompatible change is committed; bump *_min when the oldest still-supported release is retired.",
  "core": {
    "min": "5.170.0",
    "max": "5.183.1"
  },
  "backend": {
    "min": "5.65.0",
    "max": "5.75.0"
  }
}

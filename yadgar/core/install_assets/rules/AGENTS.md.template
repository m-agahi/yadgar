Yadgar v{__version__} — persistent memory engine for agentic clients.
ONE shared MCP server at `http://127.0.0.1:8765/mcp` — all clients connect here.

### Core contract (all clients)

1. **MCP endpoint**: `http://127.0.0.1:8765/mcp` — connect here for memory tools.
2. **Recall first**: before answering questions about repo structure, external
   resources, or past decisions, call
   `recall(query, project="<owner/repo>", directory=<cwd>)`. `project` is the
   scope key and is REQUIRED: yadgar never derives an identity from a directory
   (ADR-0227), so omitting it is rejected, not guessed. The SessionStart banner
   prints the value. `directory` alone resolves only when the SessionStart hook
   has registered that exact path.
   Read-first triggers also include **WebFetch / WebSearch and external API calls**.
   Triage the result: **Hit** → use it. **Miss** → do the work, then write back.
   **Drift** (memory conflicts observed state) → do the work + update memory.
   **Observed state always wins.**
3. **Write back**: after significant or multi-step work — call
   `memorize(content, project="<owner/repo>", tags=[...])`. Tag all writes.
   `project` is the ONLY scope key — it is what the stored `directory_context` is
   stamped from. `context` is a separate, OPTIONAL argument: the path of a real
   file the memory is *about*, used for nothing but staleness detection. Never
   pass a description there, and never use it for scoping.
   Also write back after any discovery that is non-obvious or reusable across sessions,
   or that contradicts prior memory; and on structural changes (new
   file/module/service). Stale = contradicted by observed state or explicitly
   deprecated → delete it.
4. **Capture routing** — one destination each: durable decision → `adr_add`;
   repo structure / convention / how a mechanism works → `wiki_add`; useful
   working fact or gotcha → `memorize`; reusable dispatch prompt →
   `agent_prompt_save`. An ADR's `decision` states the RULE, not the work that
   produced it: the ticket, branch or task that prompted it belongs in
   `context`, and may be referenced by a rule but never be its subject.
5. **Agent-Prompt Library** — before any dispatch, call
   `agent_dispatch_prelude(pattern, task_topic)` and build the dispatch on the
   returned contract; register a good reusable prompt with `agent_prompt_save(...)`.
   A guessed pattern that misses returns an EMPTY fallback, not an error — that
   is NOT proof no pattern exists. Check `wiki_read("agent-prompt-toc")` before
   concluding none fits and writing a dispatch bespoke.
6. **Yadgar findings footer**: every subagent's final message MUST end with a
   `## Yadgar findings` section (facts/insights, or "none").
7. **code_graph** (repos with it enabled) — a per-directory `code_graph` memory
   block (auto-injected) holds an indexed digest (layers/hotspots/entry-points/
   endpoints); `yadgar code-graph query <repo> <cypher>` runs a live capped
   structural query. Check it before/alongside cold grep when planning,
   exploring, or root-causing in such a repo.

### Tool reference

Every scoped tool below takes `project="<owner/repo>"`; pass it on every call.

- `memorize(content, project, tags)` — store memory with write gate.
- `recall(query, project, max_results)` — multi-signal retrieval.
- `project_brief(directory, mode, project)` — hot memories and project context.
- `checkpoint(directory, project, ...)` — snapshot working state.
- `restore(project)` — reconstruct context after compaction.
- `anchor(content, context, project, reason)` — protect critical context
  (`anchor`'s `context` IS the project directory — unlike `memorize`'s).
- `wiki_add(title, content, project, tags)` — create or update a wiki page.
- `wiki_query(query, project, tags)` — search wiki pages.
- `agent_dispatch_prelude(pattern, task_topic)` — prelude for subagent dispatch.
- `sync_instructions(claude_md_path)` — update rules file with latest content.
- `task_list(project_id, status)` — list tasks; default shape is `id` + `title`
  + `status` only, roughly a third the size of the full row.
- `task_get(project_id, id)` — read one task in full, its body included.
- `task_write(project_id, title, status, id)` — create (`id` omitted) or
  update (`id` given) a task row.

### Memory discipline

- NEVER write to `memory/` dirs or `MEMORY.md` — use Yadgar MCP tools only.
- `recall` searches episodic memory; `wiki_query` / `wiki_read` read the curated
  wiki. Use both when unsure which store holds the answer.
- Use `restore(project=...)` after `/clear` or `/compact` — NOT `recall`.
- Don't memorize: ephemeral debug findings, single-use trivia, secrets.

### Task discipline

A 2026-09-01 five-PR session that never opened the task ledger later judged
it "useful at exactly two moments — restore, and end-of-session completeness
— inert in between." Discoveries died in chat, three tasks sat `in_progress`
across four review rounds, and the same list re-rendered into context
unprompted many times over. These rules keep the ledger live in between, not
just at the edges:

- **List light**: at session start and after compaction, read `task_list` at
  its default shape (`id` + `title` + `status`), never `verbose=True`. The
  list says what exists, not what it means.
- **Read the body before acting**: before starting a task, `task_get` it and
  read its body — the reasoning, constraints and any plan live there, not in
  the title. One `task_get` call is cheap; opening the whole list is not.
- **Write the body as you go**: a decision made, a constraint found, or an
  approach rejected goes into the task's body, not only into the reply. Test:
  could someone resuming in three months act on the body alone?
- **File what you defer**: a discovery not being worked on now becomes a task
  before the session ends, with enough body to be actionable cold. Saying it
  in a report does not file it.
- **Bodies may hold plans**: a task body can carry a multi-step plan, so an
  unattended run resumes mid-plan after compaction.
- **Status tracks reality**: a task left `in_progress` while work has moved
  on is stale — split it or update the body. A status spanning hours of
  unrelated work carries no information.

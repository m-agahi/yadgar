name: sdk-js

on:
  push:
    paths: ['sdk-js/**']
  pull_request:
    paths: ['sdk-js/**']

permissions:
  contents: read

jobs:
  test:
    name: Test @yadgar/sdk
    # Self-hosted runner + yadgar-ci image (matches the rest of the
    # GitHub workflows; avoids GitHub-hosted runner minutes). The
    # yadgar-ci image has Node 22 baked (CAR 8 of the opencode port
    # train) — setup-node@v7 still pins to Node 20 for sdk-js's own
    # version-management contract; node 22 is on PATH via the image.
    runs-on: [self-hosted, linux, x64, yadgar]
    defaults:
      run:
        working-directory: sdk-js
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: sdk-js/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Verify tool coverage
        run: npm run verify-tool-coverage

  publish:
    name: Publish to GitHub Packages
    # Only runs on tag push matching sdk-js/v*
    if: startsWith(github.ref, 'refs/tags/sdk-js/v')
    needs: test
    runs-on: [self-hosted, linux, x64, yadgar]
    permissions:
      contents: read
      packages: write
    defaults:
      run:
        working-directory: sdk-js
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@yadgar'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish to GitHub Packages
        run: npm publish --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
